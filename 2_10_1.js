// if (строка с нулём) Выведется ли allert?

// Ответ: да, выведется. Все проверено, все работает, так как любая строка, кроме пустой, в типе boolean -> true 

function main() {
  if ('0') { 
  console.log('Hi!') // true
}}

main() 

