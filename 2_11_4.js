// Что выведет alert (И)? 

alert( alert(1) && alert(2) ) // Вывод - 1, затем - undefined, так как alert не возвращет значение, то есть - undefined
// поэтому до alert(2) дело просто не дойдет

function main() {

}

main()
